**Goal:** Rebuild the previous full-publishable plan into a concise, Evo-polished system — fix token ramps, mature documentation, and recursively optimise until gallery reads as a work of art.

**Architecture:** Frozen P1–P5 (tokens, hierarchy, components, patterns, motion) as guardrail. This plan optimises Phases 6–13 into 3 tight phases: ramps → parity → handoff. DTCG JSON → generated `tokens.css` → vanilla markup/CSS/JS per component → desktop gallery hosting 390px mobile frames (44px tap). Framework deferred.

**Tech Stack:** DTCG tokens + `scripts/build-tokens.mjs`, vanilla HTML/CSS/JS, `npx @google/design.md lint`, axe, `npm run lint:tokens`/`contrast`.

**Tags:** Design System, Component, Tooling

---
## 0. What this plan rebuilds (not a phase)

Supersedes `.hermes/plans/2026-09-16_193500-full-publishable-design-system.md` — same scope (pipeline → components → patterns → motion → docs → a11y → handoff) but compressed, with gaps closed: DESIGN.md only exposed 4 stops per ramp (this restores full 50–950 ramps + semantic aliases), docs were rationale-light, no swap-slot, high-contrast stub only. Locked P1–P5 decisions and `AGENTS.md` §1–5 still win; no visual change without a registry row.

---
## Phase 1 — Token ramps & pipeline rebuild {#phase-1}
*Tags: Design System, Tooling*

### Task 1: Full-ramps DTCG source (Evo parity) ✓ done
Primitives: neutral 50–950, sapphire UI + sapphire gamification (distinct keys), citrine/red-beryl/amethyst/orange/red/green 50–900 + alpha; aliases `gray/sapphire/topaz/ruby` map to them. Tier-2 semantic `bg/border/text/accent/shadow/achievement` + `dark`/`high-contrast` themes. Generator emits `design-system/tokens.css` ≤100 lines/file, header `GENERATED`.
Verify: `npm run build` byte-identical; `lint:tokens` 0 leaks; `grep primitive-.*-50` hits all ramps.

### Task 2: DESIGN.md ramp truth + swap-slot ✓ done
Frontmatter `colors:` expands to full steps per ramp (quoted hex, `{token}` refs in components); `components:` references `{colors.*}` not raw hex. Add `--signal` swap slot (`--signal:var(--accent-500)` → `--color-accent:var(--signal)`) for re-theming without renames.
Verify: `npx -y @google/design.md lint DESIGN.md` 0 errors, 0 orphans; 1 AA-large exemption re-attested.

*Shipped in 3d3f5be · Tasks 1–2 · phase-1.*

---
## Phase 2 — Component & pattern parity {#phase-2}
*Tags: Component, Layout*

### Task 3: Atoms → composites parity (≤100 lines/file, 44px) ✓ done
Atoms: Button/Chip/Badge/StatusPill/Avatar/Icon + Form (TextInput/Select/Search/Textarea/Checkbox/Radio/Switch/Slider) share one `focus-ring` + motion token. Composites: Table/ListRow/KpiStat/Timeline + Card family (Opportunity/Funnel/Profile) + GoalBar/FunnelGlyph/Sparkline. Each `var(--token)` only.
Verify: `preview.html`/`gallery.html` frame demos show all states + dark proof; axe form rules pass; ref-01–03 render with zero one-off CSS.

### Task 4: Navigation, overlays & system patterns ✓ done
TabBar (5-glyph dot), Tabs/Breadcrumbs/Pagination/Accordion; overlay behavior (focus-trap/escape/layer) for Modal/Drawer/Popover/Tooltip/Menu/Toast; Operate/Monitor screen templates + funnel 7-state machine + transient/temporal + ReportsMatrix. Motion tokens feed ElasticScroll/TiltCard + GemReward hue-rotate.
Verify: Roving-tabindex + focus-trap tests; funnel state-machine covers all 7; `prefers-reduced-motion` instant.

*Shipped in pending · Tasks 3–4 · phase-2.*

---
## Phase 3 — Docs, a11y & Evo handoff {#phase-3}
*Tags: Design System, Tooling*

### Task 5: Gallery docs maturity (Evo standard) ✓ done
Desktop gallery (full-width, `grid 220px 1fr`, left tabs) hosts 390px device frames. Per-component contract: usage + anatomy + do/don't + witness ref + a11y notes + token table + live code. Foundations doc shows full ramps live.
Verify: Every component has frame demo + `.md` contract; `prototype/gallery/*` complete; `wc -l` ≤100 each.

### Task 6: A11y + release polish loop
WCAG 2.2 AA: keyboard map, focus order, names/roles, color-independence, 360px→desktop breakpoints (raw px, `var()` invalid in `@media`). Statement + axe 0 violations. Bump `DESIGN.md` beta, re-lint, ship `CHANGELOG.md`/`CONTRIBUTING.md`/`docs/figma-parity.md` handoff; `plan:names` + `ds-track` green; recursion via `REGISTRY.md` polish passes.
Verify: `npm test` (lint + contrast + plan:names + ds-track) green; `/ds/#/changelog` Miller columns resolve; gallery opens standalone.

*Shipped in pending · Tasks 5–6 · phase-3.*

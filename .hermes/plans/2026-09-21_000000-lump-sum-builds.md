# Lump-sum builds — week of Sep 21

**Goal:** Catch-all for small shipped fixes in the week of Mon Sep 21–Sun Sep 27 2026.
**Architecture:** Additive fixes on the shipped DS; token-first, no rewrites.
**Tech Stack:** DTCG JSON → `design-system/tokens.css`; vanilla CSS/JS labs.
**Tags:** Design System, Component

---

## Phase 1 — Button lab fixes: tertiary ink, concentric tune, brand radius {#phase-1}

*Tags: Design System, Component*

### Task 1: accent + dark tertiary foreground ✓ done
Accent tertiary used body ink; dark tertiary reused light strong tones (fail on dark).
Now one ink map (`button-lab-tone.js`): light accent → action-strong,
dark every hue → action-soft, success/warning keep 700s in light only.

### Task 2: concentric radius/padding tune bar ✓ done
New `data-blab-tune` bar appears only with Concentric on: pad min–max
(vertical/horizontal nest gap, 0–24) + radius min–max (button/box, 4–32
brand max). Applied live on the demo box; box stays demo-only, out of snippet.

### Task 3: brand radius max (full 9999 → 32) corrected everywhere ✓ done
`radius.full` was the 9999px pill hack; brand max is 32 (`radius.3xl`).
Retokened to 32px, rebuilt tokens, fixed radius docs + `top-pills.jsx`.

### Task 4: full-width preview stretch ✓ done
`btn--full-width` rendered shrink-wrapped (preview is flex-centered).
Preview now stretches; full-width buttons fill it, incl. inside the demo box.

*Shipped in fc4b00f · Tasks 1–4 · phase-1.*

---

## Phase 2 — Code syntax highlight: IDE mono blocks for DS docs {#phase-2}

*Tags: Design System, Component*

Token-driven `ds-code` blocks wherever code is shown — foundations, components, playgrounds — one highlight path, no hardcodes.

### Task 5: code tokens + highlight engine + chrome ✓ done
Tokens `color-code-bg/fg/comm/str/tag/attr/kw/var/num/punct` via `var(--primitive-*)` (light) + `300` ramps (dark); engine `src/ds/code-highlight.js` (`highlight` + `codeBlock` with __HLn__ stash); chrome `src/ds/code-highlight.css` (`.ds-code`, `code.hl`, `.hl-*` via `var(--color-code-*)`, `data-lang` badge, mono + scroll).

### Task 6: adopt across DS docs + button lab ✓ done
Static `code.tp-code` in `public/foundations/*`, `public/component-docs/*`, gallery panels auto-highlighted via `code-highlight.js` (runtime `innerHTML` on `code.hl`, not `textContent`); `button-lab.js` `data-blab-code` uses `highlight()` for the live snippet while copy stays raw.

*Shipped in <sha> · Tasks 5–6 · phase-2.*
---

## Phase 3 — Component docs loop: per-component intro · lab · spec {#phase-3}

*Tags: Design System, Component*

Token-first mirror of button lab for every remaining component — 7 parallel groups (identity · form · choice · nav · overlays · data · cards), 32 components, 3 docs per component.

### Task 7: Identity — Chip · Badge · StatusPill · Avatar · Icon ✓ done
4 new intros (badge/statuspill/avatar/icon) + 5 labs (chip/badge/statuspill/avatar/icon) via `ilab` shell (`identity-lab.css` 31 + per-component 2–5) + 5 spec appendices (4 specimens single-column, tables only) + 10 JS/CSS lab logic (≤60). Token-only, no hex, build passes.

### Task 8: Form — TextInput · Select · Search · Textarea ✓ done
3 new intros (select/search/textarea) + 4 labs (textinput/select/search/textarea) via `flab` shell (`form-lab.css` 25 + per-component 2–3) + 5 specs + 8 JS/CSS. Live `field__wrap`, `select__chevron`, `search__clear`, `textarea__count`. Token-only.

### Task 9: Choice — Checkbox · Radio · Switch · Slider ✓ done
2 new intros (radio/switch, slider patched from form to choice) + 4 labs (`clab` shell, size/state/checked/indeterminate/error, `role=switch`, `type=range` discrete ticks) + 4 specs + 8 JS/CSS. `el.indeterminate=true` for partial select-all.

### Task 10: Navigation — Tabs · Breadcrumbs · Pagination · Accordion · TabBar · Toolbar ✓ done
5 new intros (breadcrumbs/pagination/accordion/tabbar/toolbar) + 6 labs (`nlab` shell, roving tabindex, collapse `…`, page math, `aria-expanded`) + 6 specs + 12 JS/CSS.

### Task 11: Overlays — Modal · Drawer · Menu · Popover · Tooltip · Toast ✓ done
Verify `overlays-intro` (6 layers, 3 laws) + 6 per-layer labs (`olab` shell, size/side/placement/tone, `ov-stage` scoped preview) + 6 specs + `overlay-lab` shared 29 + 6 per-layer JS (≤25). No hex, stage-local not fixed viewport.

### Task 12: Data — Table · ListRow · KpiStat · Timeline · Sparkline ✓ done
3 new intros (listrow/kpi/timeline, sparkline) + 5 labs (`dlab` shell, `aria-sort` single-column, skeleton shimmer, rail 10px/6px pixel-identical, horizontal) + 5 specs + 10 JS/CSS.

### Task 13: Cards — OpportunityCard · FunnelCard · ProfileCard · GoalBar · FunnelGlyph · GemReward ✓ done
5 new intros (funnelcard/profilecard/goalbar/funnelglyph/gemreward) + 6 labs (`carlab` shell, hairline `border.thin` zero shadow, stage canvas/surface/wash + light/dark, gem quarantine) + 7 specs + 14 JS/CSS.

### Task 14: Gallery wiring — scannable per-component pages ✓ done
New `src/ds/labs.css` (35-line barrel @import) + `src/ds/labs.js` (40-line barrel side-effects) + `gallery.html` rewired: 8 frames each one-line with per-component `data-doc` pads (115 fetches) — identity 15, text 12, choice 12, nav 18, overlays 13, data 15, cards 18. Single `labs.css`/`labs.js` keeps gallery under 100 lines; `gallery-shell.js` `ds:doc` boot covers every lab.

*Shipped in 44c2bab · Tasks 7–14 · phase-3.*

## Phase 4 — Gallery quick-jump: left sub-rails for every section {#phase-4}

*Tags: Design System, Component*

Collapsible per-section shortcut rails under the left nav — one click jumps to any component intro, URL deep-links it.

### Task 15: Identity sub-tabs ✓ done
Rail under Identity (Chip · Badge · Status pill · Avatar · Icon) + anchor ids on intro pads + `dsSub` scroll/active/hash in `gallery-shell.js` + `gallery-subnav.css` (33, token-only).

### Task 16: All-section quick-jump rails ✓ done
`src/ds/gallery-subnav.js` (53) builds all 7 rails from one map (36 shortcuts: identity 5 · text 4 · choice 4 · nav 6 · overlays 6 · data 5 · cards 6); 36 anchor ids in `gallery.html`; shell slimmed (94→82) with panel-safe hash init; rail shows only for the open section; deep hash scrolls once via `ds:doc` guard.

*Shipped in 73d7b2e · Tasks 15–16 · phase-4.*

### Task 17: Nav IA + independent rail scroll ✓ done
`.dnav` capped at viewport height with its own scroll (`overscroll-behavior:contain`, `gallery-subnav.css` 37); Motion deduped to Foundations, Gems moved into Patterns, "Motion & Gamification" heading removed (`gallery.html` nav only).

*Shipped in 9f1874a · Task 17 · phase-4.*

### Task 18: Procedural gems replace dead Spline embeds ✓ done
Spline URL 403'd (AccessDenied XML in every frame) → pure `gem-cut.js` (41, zero-dep brilliant math: 16-facet girdle/table/culet, token + hue + spin per category) + `gem3d.js` (76, THREE physical flat-shade, token-resolved color, rAF spin, reduced-motion still) + `prototype/gems/` Python port (`gem_cut.py` + matplotlib `render_gems.py`, exact token hexes from CSS, preview PNG proves parity). 10 canvases wired (4 Patterns + 6 cards spec), docs de-splined, atlas regen.

*Shipped in 86abb4b · Task 18 · phase-4.*

## Phase 5 — Three.js gem: smooth tier {#phase-5}

*Tags: Motion, Component*

Task 18's flat 16-facet canvas render rated 3/10 as a premium gem: no environment to reflect (no glints), dark `-500` body tokens, and stale iframe-era `hue-rotate` filters distorting 3 of 4 categories. This phase lifts the same procedural cut to the dazzling tier — proper round brilliant (smooth girdle silhouette, star / upper-girdle / lower-girdle facets), vivid gamification tokens (`-400` ramp), procedural PMREM studio env + physical material + ACES + contact pool, smooth float/spin/parallax motion — and keeps the portable-core promise with a numpy Python twin rendering the same cut (JS ⇄ PY parity).

### Task 19: Round-brilliant cut v2 + vivid tokens + JS⇄PY parity ✓ done
`gem-cut.js` (79) rebuilt: 48-column silhouette, octagon table, 8 star + 8 bezel kites, upper-girdle fans, thin girdle band, 8 pavilion mains + 16 lower-girdle facets, culet — 136 verts / 268 tris, notch-free (≤0.28 px chord at 2×), vivid `-400` tokens + legacy `red-beryl` alias. `gem_cut.py` (99) mirrors it; `parity_check.py` (50) asserts identical order ≤1e-9 (maxDelta 1.1e-16).

### Task 20: Headless shot/rating rig ✓ done
`prototype/gems/gem-shot.mjs` (99): CDP rig — navigate → wait → probe (THREE revision, mount count, 404s, console errors, fps) → full-page + per-selector 1:1 clips. Every rating round ran through it (3 → 6 → 7 → 8 → 8.5/10).

### Task 21: Smooth renderer — studio env, physical material, transmission, motion ✓ done
`gem-env.js` (59): procedural PMREM studio (dark shell, softboxes, jittered 4-band × 7-azimuth emitter tent, strips, sparks). `gem-scene.js` (75): winding-fixed geometry + MeshPhysicalMaterial (roughness .06 · ior 2.4 · clearcoat · iridescence .45 · env 1.6 · emissive .13), bbox camera fit FILL .75, transmission .9 + attenuation on GPU — off on software (`?gem=high` forces). `gem3d.js` (95): ACES, dt spin/float/tilt, IntersectionObserver mounts, dispose. `gem-fallback.js` (39): token 2D gem when THREE absent. `gem-reward.css` hue-rotate filters removed (citrine verified 10/10); lab iframe → canvas.

### Task 22: Python twin renderer + portable README ✓ done
`soft_render.py` (100): numpy z-buffer (Newell normals, barycentric, 2× SS, Lambert + Blinn-Phong + rim) → `gems-preview.png`; `render_gems.py` (91) studio sheet + contact shadows on the shipped cut; `README.md` (80) portable-core story + port guide.

*Shipped in 86abb4b · Tasks 19–22 · phase-5.*
